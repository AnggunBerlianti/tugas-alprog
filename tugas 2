#include <stdio.h>
#include <string.h>

int main() {
    char s1[101], s2[101];

    // Membaca dua string
    scanf("%s", s1);
    scanf("%s", s2);
    
    // Memeriksa panjang kedua string
    if (strlen(s1) != strlen(s2)) {
        printf("BERBEDA\n");
    } else {
        // Jika panjang sama, periksa apakah kedua string identik
        if (strcmp(s1, s2) == 0) {
            printf("IDENTIK\n");
        } else {
            printf("MIRIP\n");
        }
    }

    return 0;
}